int func(vector<int> &nums, int x){
  int right = x-1, left = 0;
  while(right < nums.size()-1){
      sum = sum - arr[l];
      l++;
      r++;
      sum = sum + arr[r];
      ans = max(ans, sum);
  }
  return ans;
}
